<!DOCTYPE html>
<html lang="en">
  <meta charset="utf-8">
  <title>NUSMods</title>
  <meta name="viewport" content="width=device-width,height=device-height,user-scalable=no,initial-scale=1,minimum-scale=1">
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css" />
  <link rel="manifest" href="/manifest.json">
  <base href="/"/>
  <script>
var request = new XMLHttpRequest();
request.open('GET', 'http://modsplanner.tk:3001/static/boolector.js', false);
request.send(null);
boolector = request.responseText;
function createBoolector() {
  eval(boolector)
  return Module;
}
var BoolectorModuleArray = [createBoolector(), createBoolector()];
  </script>
</head>
<body>
  <div id="app"></div>
  <% if (htmlWebpackPlugin.options.dll) { %>
    <% htmlWebpackPlugin.options.dll.paths.forEach(function (path) { %>
      <script src="<%= path %>"></script>
    <% }) %>
  <% } %>
</body>
</html>
